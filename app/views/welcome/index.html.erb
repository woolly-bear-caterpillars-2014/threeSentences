<div class="container">
  <div class="absolute-center">
    <% unless user_signed_in? %>
      <h1 id="brand">three sentences</h1>
    <% end %>
    <% if user_signed_in? %>
    <div id="profile">
      <h2><%= @user.username %>'s stories</h2>
        <ol>
          <li><%= link_to 'New Story', new_story_path, class: 'button newstory' %></li>
          <% @user.stories.each do |story| %>
            <li><%= link_to story.name, story, class: 'button' %></li>
          <% end %>
        </ol>
    </div>
    <% else %>
      <p id="tagline">If you can write three sentences,<br> you can write a story.</p>
    <% end %>
  </div>
</div>

<!-- link_to "Delete", story_path(story), method: :delete -->

